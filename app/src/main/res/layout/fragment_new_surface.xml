<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".motion.responsive.create.NewSurfaceFragment">

    <android.support.v7.widget.CardView
        android:id="@+id/fragment_new_surface_card"
        android:layout_width="160dp"
        android:layout_height="160dp"
        android:layout_above="@+id/fragment_new_surface_toggle"
        android:layout_alignLeft="@id/fragment_new_surface_toggle"
        android:visibility="gone"
        app:cardBackgroundColor="@android:color/white" />

    <ImageButton
        android:id="@id/fragment_new_surface_toggle"
        style="@style/MotionInAndroidButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_centerInParent="true"
        android:layout_marginBottom="@dimen/spacing_large"
        android:layout_marginLeft="@dimen/spacing_large"
        android:layout_marginTop="@dimen/spacing_medium"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_add_black_24dp" />

</RelativeLayout>
